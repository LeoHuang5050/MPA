# Monad 蜂群思维 (Hivemind)

[![Monad Hackathon](https://img.shields.io/badge/Monad-Hackathon-purple)](https://monad.xyz)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## 📖 项目简介

**Monad Hivemind** 是一个专为 Monad 高性能链设计的 **AI 驱动的高频套利与交易代理系统**。

本项目想要通过监控 Monad 的出块来监控巨鲸交易，当有巨鲸交易导致流动性剧烈波动时，立即发起询价，捕捉 DEX 之间的套利空间。系统利用最新的账户抽象技术和 AI 路径算法，实现了全自动、无风险的链上获利。

## ✨ 核心功能

### 1. 🔍 巨鲸监控与套利发现 (Whale & Arbitrage Monitoring)
系统实时监听 Monad 网络的区块与 Mempool，识别会产生巨大滑点的大额交易（Whale Transactions）。
- **机制**: 当监测到巨鲸买入/卖出时，AI 引擎立即计算由此产生的流动性不平衡。
- **目标**: 抢在市场平配之前，发现无风险套利路径。

### 2. 🤖 代理账户与免签执行 (Agent Direct Execution)
解决了传统 DeFi 交易需要用户频繁签名确认的痛点。
- **技术栈**: 基于 **ERC-4337 (账户抽象)** 标准构建的 `HiveAccount` 智能账户。
- **实现**: 类似于 **EIP-7702** 或 Session Keys 的设计理念，用户授权 "AI Agent" 地址作为 `Executor`。
- **效果**: AI 可以在用户设定的资金限额内，**无需用户签名** 直接发起并执行高频套利交易，真正实现全自动化。

### 3. 🛡️ 原子性与回滚保护 (Atomic Execution)
利用 Monad 交易的 **原子性 (Atomicity)** 确保资金绝对安全。
- **零风险**: 交易在一个原子事务中完成（闪电贷 -> 买入 -> 卖出 -> 还款）。
- **回滚机制**: 如果最终利润低于预期（例如因抢跑或滑点导致亏损），合约逻辑会自动 `Revert`（回滚）。
- **结果**: 交易失败仅损失极低的 Monad Gas 费，本金完全无损。

### 4. 🧠 AI 智能路由代理 (AI-Agent Pathfinding)
一个简单的 AI Agent 助手，通过自然语言或意图触发。
- **场景**: "帮我把 USDC 换成 MON，寻找最优报价"。
- **能力**: 自动扫描链上所有 DEX（如 Uniswap V3, Kuru 等），通过 Graph 算法（如 Bellman-Ford 或 DFS）寻找最优兑换路径或套利环路。

---

## 🛠 技术栈 (Tech Stack)

### 前端 (Frontend)
*   **Framework**: Next.js (App Router)
*   **UI Library**: Scaffold-ETH 2, TailwindCSS, DaisyUI
*   **Web3 Integration**: Wagmi, Viem, RainbowKit

### 智能合约 (Smart Contracts)
*   **Solidity**: v0.8.20
*   **Framework**: Hardhat
*   **Standards**: ERC-4337 (Account Abstraction), ERC-20
*   **Key Contracts**: 
    *   `HiveAccount.sol`: 核心智能钱包逻辑
    *   `ArbitrageExecutor.sol`: 原子化套利执行器

### AI & 后端 (AI Integration)
*   **Python**: Flask (API 服务)
*   **Graph Processing**: NetworkX (用于计算 DEX 路径)
*   **Data Analysis**: Pandas (市场数据处理)
*   **Web3.py**: 链上数据交互与 Multicall 聚合查询

---

## 🚀 快速开始 (Quick Start)

### 环境依赖
*   Node.js >= v20
*   Yarn (v1 或 v2+)
*   Python 3.9+
*   Git

### 1. 安装项目依赖
```bash
# 安装根目录及所有包的依赖
yarn install
```

### 2. 启动本地链 (Monad Fork)
启动本地 Hardhat 网络，模拟 Monad 环境。
```bash
# 终端 1
yarn fork
```
> **注意**: `yarn fork` 会开启主网 Fork 模式。如果只想开启空链，可以使用 `yarn chain`。

### 3. 部署智能合约
将 `HiveAccount` 和其他基础设施部署到本地链。
```bash
# 终端 2
yarn deploy
```

### 4. 启动 AI 发现引擎 (Python Discovery)
这是 Hivemind 的核心大脑，负责计算套利路径。
```bash
# 终端 3
cd python-discovery

# 建议创建虚拟环境 (可选)
python3 -m venv venv
source venv/bin/activate  # Mac/Linux
# .\venv\Scripts\activate # Windows

# 安装 Python依赖
pip install -r requirements.txt

# 启动服务
python3 app.py
```
> 服务将在 `http://127.0.0.1:5001` 启动。

### 5. 启动前端应用
```bash
# 终端 4
cd packages/nextjs
yarn start
```
或者在根目录直接运行 `yarn start` (这会同时启动 Next.js)。

打开浏览器访问 `http://localhost:3000` 即可开始使用 Hivemind。

---

## 📜 常用命令速查

| 命令 | 描述 |
| --- | --- |
| `yarn fork` | 启动带 Mainnet Fork 的本地节点 (推荐) |
| `yarn chain` | 启动空白本地节点 |
| `yarn deploy` | 部署合约到当前网络 |
| `yarn start` | 启动 Next.js 前端 |
| `yarn compile` | 编译 Solidity 合约 |
| `yarn test` | 运行合约测试用例 |

## 📜 License
MIT